const config = {
    env: process.env.NODE_ENV,
    port: process.env.PORT,
    dbUri: process.env.URI,
    privateKey: process.env.PRIVATE_KEY,
    cookie:process.env.ENCRIPT,
}

export default config;